# Claude Memory Compatibility

Autopilot reads this file as project memory, alongside `AUTOPILOT.md` and `PostTrainingAgent.md`. Keep durable project rules here when you want both Claude-style coding agents and the post-training agent to share the same context.
Always keep 2x H800 GPUs fully utilized, to avoid server warnings.